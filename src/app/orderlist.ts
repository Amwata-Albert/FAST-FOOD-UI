export class Orderlist {
    showDescription: boolean;
    constructor(public order_id: number,public food: string, public quantity: number, public address: string
    ){
    ;this.showDescription=false
  }
}
    

